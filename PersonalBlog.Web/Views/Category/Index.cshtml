@model List<CategoryDto>

<div class="p-5 bg-black mb-5" style="background-color:background-color: rgba(255,255,255,0.02);">
    <h3 style="color: burlywood; font-weight: bolder; padding: ;"><a asp-controller="Home" asp-action="Index" class="btn btn-dark ms-auto me-4">
            <i class="fa-solid fa-arrow-left"></i></a>Kategoriler</h3>
            @foreach (var category in Model)
            {
                <div class="my-4">
                    <a asp-action="" asp-controller="" asp-route-id="">@category.Name</a>
                    <a class="text-warning" asp-action="Edit" asp-route-id="@category.Id">Düzenle</a>
                    <a class="text-danger" asp-action="Delete" asp-route-id="@category.Id">Sil</a>
                    <br>
                </div>
            }
</div>